@model IEnumerable<CourseRegistration.Models.Course>

@{
    ViewBag.Title = "SearchResult";
}

<h2>Search <i><small>@Model.Count() results</small></i></h2>

<hr />
@foreach (var item in Model)
{
    //if (item.enabled)
    //{
    <h4>@Html.ActionLink(item.CourseTitle, "Details", "Course", new { code = item.CourseCode }, new { })</h4>
    <p>
        @if (item.CourseDescription == null || item.CourseDescription == "")
        {
            @Html.Display("The description of this course is nothing now.")
        }
        else
        {
            @Html.DisplayFor(modelItem => item.CourseDescription)
        }
    </p>
    <p><mark>Fee: @Html.DisplayFor(modelItem => item.Fee) | Duration: @Html.DisplayFor(modelItem => item.NumberOfDays)</mark></p>
       
    <hr />
    //}
}

@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CourseTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CourseDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fee)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumberOfDays)
        </th>
    </tr>

@foreach (var item in Model)
{
    //if (item.enabled)
    //{
        <tr>
            <td>
                @Html.ActionLink(item.CourseTitle, "Details", "Course", new { code = item.CourseCode }, new { })
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CourseDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fee)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumberOfDays)
            </td>
        </tr>
    //}
}

</table>*@
